function compare(num1, num2) {
    if ( num1 > num2) {
        console.log(num1 + " is bigger " + num2);
    } else if ( num1 < num2) {
        console.log(num2 + " is bigger " + num1);
    } else {
        console.log("both are the same");
    }
};

compare(300, 30);